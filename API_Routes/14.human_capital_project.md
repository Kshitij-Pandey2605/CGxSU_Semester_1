# Human Capital

---

# Basic GET Routes

```http
GET /prices
GET /prices/:countryCode
GET /prices/year/:year
GET /prices/month/:month
GET /prices/indicator/:indicator

GET /prices/frequency/:freq
GET /prices/country/:countryCode
GET /prices/country/:countryCode/year/:year
GET /prices/country/:countryCode/month/:month
GET /prices/latest
GET /prices/recent
GET /prices/random
GET /prices/trending
```

---

# Route Parameters Practice

```http
GET /prices/country/:countryCode

GET /prices/year/:year

GET /prices/month/:month

GET /prices/indicator/:indicator

GET /prices/value/:value

GET /prices/frequency/:freq

GET /prices/range/:startYear/:endYear

GET /prices/country/:countryCode/year/:year

GET /prices/country/:countryCode/month/:month

GET /stats/country/:countryCode

GET /stats/year/:year
```

---

# Query Parameter Practice

```http
GET /prices?year=2000
GET /prices?month=1

GET /prices?country=ABW&year=2000

GET /prices?minValue=50
GET /prices?maxValue=100

GET /prices?minValue=50&maxValue=100

GET /prices?indicator=FAO_CP_23012

GET /prices?freq=M

GET /prices?country=IND&month=5

GET /prices?country=USA&year=2020&month=6

GET /prices?sort=value&order=desc

GET /prices?fields=country,year,value

GET /prices?search=consumer

GET /prices?contains=price

GET /prices?exactYear=2005
```

---

# Pagination Routes

```http
GET /prices?page=1&limit=10

GET /countries?page=2&limit=5

GET /years?page=1&limit=20

GET /prices/ABW?page=1&limit=5

GET /prices?page=2&limit=25

GET /prices?page=5&limit=50

GET /prices/latest?page=1&limit=10

GET /stats/countries?page=1&limit=15
```

---

# Sorting APIs

```http
GET /prices?sort=value

GET /prices?sort=-value

GET /prices?sort=year

GET /prices?sort=month

GET /prices?sort=country

GET /prices?sort=year,-value

GET /prices?sort=country,year

GET /prices?sort=month,-year

GET /prices?sort=indicator

GET /prices?sort=freq

GET /prices?sort=createdAt
```

---

# Search APIs

```http
GET /search/country?name=Aruba

GET /search/indicator?text=Consumer

GET /search?country=Aruba&year=2000

GET /search/value?value=68

GET /search/month?month=1

GET /search/year?year=2020

GET /search/frequency?freq=M

GET /search/prices?q=general

GET /search/prices?q=consumer

GET /search/prices?q=price

GET /search/countries?q=india

GET /search/indicators?q=index
```

---

# CRUD Operations

```http
POST /prices

PUT /prices/:id

PATCH /prices/:id

DELETE /prices/:id

GET /prices/:id

PATCH /prices/value/:id

PATCH /prices/month/:id

PATCH /prices/year/:id

DELETE /prices/country/:countryCode

POST /prices/bulk-insert

DELETE /prices/delete-all
```

---

# Example POST Body

```json
{
  "REF_AREA": "IND",
  "REF_AREA_LABEL": "India",
  "Value": "120",
  "Year": "2024",
  "Month": "5"
}
```

---

# Advanced Filtering Routes

```http
GET /prices/filter?year=2000&month=1&country=ABW

GET /prices/filter?startYear=2000&endYear=2005

GET /prices/filter?months=1,2,3

GET /prices/filter?countries=ABW,IND,USA

GET /prices/filter?minValue=40&maxValue=100

GET /prices/filter?freq=M

GET /prices/filter?indicator=FAO_CP_23012

GET /prices/filter?year=2020&sort=value

GET /prices/filter?country=IND&month=5

GET /prices/filter?country=USA&year=2022&month=8
```

---

# Statistics Routes

```http
GET /stats/average-value

GET /stats/highest-value

GET /stats/lowest-value

GET /stats/country/:countryCode

GET /stats/year/:year

GET /stats/month/:month

GET /stats/median

GET /stats/mode

GET /stats/top-countries

GET /stats/top-indicators

GET /stats/monthly-average

GET /stats/yearly-average

GET /stats/value-distribution

GET /stats/summary
```

---

# Middleware Practice Routes

```http
GET /admin/prices

POST /admin/prices

DELETE /admin/prices/:id

PATCH /admin/prices/:id

GET /admin/dashboard

GET /protected/prices

POST /protected/prices

DELETE /protected/prices/:id

PATCH /protected/prices/:id
```

---

# Authentication Practice Routes

```http
POST /auth/register

POST /auth/login

POST /auth/logout

GET /profile

PATCH /profile

POST /auth/forgot-password

POST /auth/reset-password

POST /auth/refresh-token

GET /auth/me

DELETE /auth/account
```

---

# JWT Authentication Routes

```http
GET /jwt/profile

GET /jwt/dashboard

POST /jwt/generate-token

POST /jwt/verify-token

POST /jwt/refresh-token

GET /jwt/admin

GET /jwt/user

DELETE /jwt/logout
```

---

# Error Handling Practice Routes

```http
GET /prices/:id
→ Handle invalid ID
→ Handle record not found

POST /prices
→ Handle missing fields
→ Handle invalid data types

PATCH /prices/:id
→ Handle invalid updates

DELETE /prices/:id
→ Handle already deleted records

GET /admin/prices
→ Handle unauthorized access

POST /auth/login
→ Handle invalid credentials
```

---

# Request Validation Practice

```http
POST /prices

→ Validate Year is numeric

→ Validate Month between 1-12

→ Validate Value is numeric

→ Validate REF_AREA required

PATCH /prices/:id
→ Validate updated fields

POST /auth/register
→ Validate email/password

POST /auth/login
→ Validate credentials

GET /prices/filter
→ Validate query parameters
```

---

# API Rate Limiting Practice

```http
GET /prices
→ Limit requests per minute

POST /auth/login
→ Prevent brute force attacks

POST /auth/register
→ Limit registration requests

GET /search/country
→ Limit excessive searches

GET /admin/dashboard
→ Strict admin rate limiting

POST /prices
→ Prevent spam creation

DELETE /prices/:id
→ Limit delete requests
```

---

# Export & Import Routes

```http
GET /export/json

GET /export/csv

GET /export/excel

POST /import/json

POST /import/csv
```

---

# Bonus Practice Routes

```http
GET /prices/top/highest

GET /prices/top/lowest

GET /stats/monthly-average

GET /compare?country1=IND&country2=USA

GET /compare/year?year1=2000&year2=2020

GET /prices/random

GET /prices/trending

GET /prices/recent

GET /prices/latest

GET /health

GET /version
```

---
